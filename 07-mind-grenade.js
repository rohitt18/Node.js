const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is : ${num1 + num2}`);
}

addValues() // when you're calling or executing a function then it will still work without exporting
// just import it by saying require('./07-mind-grenade.js) in the app.js


// Till here, we created our first module & in the process we covered the general module syntax 
// and use cases as well.
