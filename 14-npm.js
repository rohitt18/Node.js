// Node Package Manager.

// A package essentially is a folder that contains a javascript code
// At the end of the day package, dependencies & modules all three are used interchangeably when talking about shareable JS code
// They all mean the same thing


// npm - global command, comes with node 
// npm --version

// local dependency - use it only in this particular object
// npm i <packageName>

// global dependency - use it in any project
// npm install -g <packageName>
// sudo npm install -g <packageName> mac

// more often used one is definitely local dependency
